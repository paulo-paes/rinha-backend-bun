{
  "name": "app",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "bun build src/index.ts --target bun --compile --outfile dist/app"
  },
  "dependencies": {
    "elysia": "latest",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "@types/pg": "^8.11.2",
    "bun-types": "latest"
  },
  "module": "src/index.js"
}